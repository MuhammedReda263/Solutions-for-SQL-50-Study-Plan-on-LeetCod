with newTable as (
    select num , lag(num,1) over (order by id) as prev , lead(num,1) over (order by id) as next
    from Logs 
)

select distinct num as ConsecutiveNums
from newTable
where num = prev and num = next