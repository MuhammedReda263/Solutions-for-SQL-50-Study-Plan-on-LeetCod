with new_table as (
select customer_id , count(distinct product_key) as products_count
from Customer 
group by customer_id 
),
 count_Product_table as (
    select count(product_key) as product_count
    from Product
)
select customer_id
from new_table n , count_Product_table p
where n.products_count = p.product_count